# Простая инструкция: несколько ККТ, ЕСМ, контроллеры и ЛМ ЧЗ

Издатель и владелец: KRS. Автор: Руслан Керусов. Copyright © 2026 KRS. Все права защищены.

## 1. Что умеет программа

Есть два основных способа работы:

- `Ручное подключение` — добавить и зарегистрировать вторую, третью и следующие ККТ по одной;
- `Автоматический режим` — последовательно настроить весь комплект: ККТ в ЕСМ, отдельный контроллер для каждой ККТ и отдельный ЛМ ЧЗ для каждого уникального ИНН.

Для просмотра и удаления используются вкладки `ККТ в ЕСМ` и `ЛМ ЧЗ`.

Если несколько касс принадлежат одному ИНН, они используют один ЛМ ЧЗ, но у каждой остаётся свой контроллер. Если ИНН разные, для каждой кассы получится отдельный ЛМ.

## 2. Что подготовить

1. Распакуйте весь компактный архив программы в защищённую папку:

   ```text
   C:\Program Files\KRS\MultiKKT
   ```

   Из `Downloads`, рабочего стола и других доступных на запись папок управление службами блокируется.

2. Убедитесь, что установлены и запущены ЕСМ/ТС ПИоТ и драйвер АТОЛ, а нужные физические ККТ подключены.

3. Подготовьте два официальных файла:

   ```text
   esm-lm-controller_1.6.3.2-windows-setup.exe
   regime-2.6.1-7.msi
   ```

   Программа принимает только эти проверенные версии. Файлы поставщиков в комплект MultiKKT не входят.

4. Проверьте верхнюю строку программы. Обычные значения:

   ```text
   Адрес ЕСМ: http://127.0.0.1:51077
   dkktPort: 4042
   ```

   `4041` — порт службы АТОЛ для ККМ. Для оркестра ДККТ ЕСМ обычно нужен именно `4042`.

## 3. Полная автоматическая настройка

Это основной сценарий: пользователь запускает настройку и получает готовые локальные службы без ручного клонирования каталогов и регистрации служб.

1. Подключите все физические ККТ и убедитесь в драйвере АТОЛ, что видны именно нужные кассы.
2. Откройте вкладку `Автоматический режим`.
3. Выберите установщик контроллера и MSI ЛМ ЧЗ. Если файлы ещё не выбраны, программа сама попросит их указать при запуске.
4. Нажмите `Настроить всё автоматически`.
5. Проверьте предварительную таблицу. В ней показаны только нужные человеку данные:

   - номер ККТ;
   - серийный номер;
   - ИНН;
   - порт кассового ПО (`softPort`);
   - адрес и порт ЛМ ЧЗ;
   - результат проверки.

6. Обычно ничего менять не нужно. По умолчанию всё работает на этом компьютере:

   | № | Порт кассового ПО | Endpoint ЛМ ЧЗ |
   | ---: | ---: | --- |
   | 1 | `51401` | `127.0.0.1:5995` |
   | 2 | `51402` | `127.0.0.1:6995` |
   | 3 | `51403` | `127.0.0.1:7995` |

   Реальный `softPort` берётся из ЕСМ. Если две ККТ имеют один ИНН, endpoint ЛМ у них будет одинаковым. Полный автоматический режим создаёт ЛМ на этом компьютере, поэтому адрес `127.0.0.1` фиксирован; API-порт можно изменить до запуска. Внутренние порты контроллеров, базы и EPMD программа назначает и проверяет сама.

7. Подтвердите лицензионное предупреждение и один запрос UAC.

Дальше программа работает последовательно:

1. проверяет оба выбранных пакета и свободные новые порты;
2. добавляет и регистрирует очередную ККТ в ЕСМ, если она ещё не зарегистрирована;
3. создаёт или проверяет ЛМ для ИНН этой ККТ;
4. создаёт и запускает отдельный контроллер этой ККТ;
5. только после успешной первой контрольной ККТ переходит к следующим.

Если первая контрольная ККТ не настроилась, остальные не затрагиваются. Ошибка следующей независимой ККТ не стирает уже готовые комплекты. Кнопка `Остановить` прекращает работу после текущей безопасной операции.

Успешный итог означает: службы ЛМ и контроллера запущены, ЛМ готов к инициализации. Саму бизнес-инициализацию выполняет ЕСМ либо сторонняя утилита; MultiKKT не вводит логины и пароли и не открывает ссылки.

## 4. Где смотреть результат

Откройте вкладку `ЛМ ЧЗ` и нажмите `Обновить`. Основная таблица показывает:

- № ККТ;
- серийный номер;
- ИНН;
- порт кассового ПО;
- адрес и порт ЛМ ЧЗ;
- состояние ЛМ;
- состояние связи с ЕСМ.

Этой таблицы достаточно специалисту, чтобы сопоставить кассовое ПО, физическую ККТ и нужный ЛМ. Имена служб, пути, хэши и внутренние DB/gRPC/REST/EPMD-порты в таблице не показываются; они остаются в диагностическом журнале.

На этой же вкладке можно выбрать оба пакета и нажать `Настроить все ККТ`. Это тот же полный сценарий, что и на вкладке автоматического режима.

## 5. Ручное подключение одной ККТ

Используйте этот путь, если хотите добавлять кассы по одной и лично проверять каждую физическую ККТ.

1. Нажмите `Проверить` в верхней строке.
2. На вкладке `Ручное подключение` нажмите `1. Выбрать следующую ККТ`.
3. Если найдено несколько новых ККТ, выберите нужную по серийному номеру, ФН и ИНН.
4. Проверьте автоматически выбранные `port` и `softPort`. На чистой ЕСМ первая ККТ получает `50401/51401`, вторая — `50402/51402` и так далее. Если первая ККТ уже создана, её пара не выдаётся повторно.
5. Закройте Frontol или другое кассовое ПО, если оно удерживает порт.
6. Нажмите `2. Добавить экземпляр`.
7. В драйвере АТОЛ переключитесь именно на подключаемую физическую ККТ и проверьте связь.
8. Установите флажок подтверждения и нажмите `3. Зарегистрировать ККТ`.
9. Убедитесь, что нижняя таблица обновилась, затем повторите действия для следующей кассы.

## 6. Удаление и повторная попытка

### Дополнительная ККТ в ЕСМ

На вкладке `ККТ в ЕСМ` выберите дополнительный экземпляр и нажмите `Удалить дополнительную ККТ`. Первая ККТ защищена. Удаление подтверждается последними четырьмя цифрами серийного номера и считается успешным только после повторной проверки списка ЕСМ.

### Один локальный комплект

На вкладке `ЛМ ЧЗ` выберите строку и нажмите `Удалить выбранный комплект`. Удаляются контроллер этой ККТ и принадлежащий программе профиль. Если другой комплект того же ИНН ещё использует общий ЛМ, сам ЛМ сохраняется.

### Всё созданное программой

Нажмите `Удалить всё созданное`, проверьте список и введите фразу подтверждения `УДАЛИТЬ ВСЕ`. Один helper/UAC удалит только повторно подтверждённые объекты MultiKKT: службы, профили, данные, журналы и неиспользуемый runtime.

Штатные службы поставщика, чужие каталоги и записи ККТ в ЕСМ эта команда не удаляет. Если Windows ещё держит файл или завершает удаление службы, появится состояние `Требуется очистка`. После перезагрузки нажмите `Завершить очистку`; вручную искать остатки не нужно.

## 7. Частые ошибки

- `1010` — экземпляр уже существует; программа пытается продолжить незавершённую регистрацию без повторного добавления.
- `1012` или `1013` — служба экземпляра ЕСМ не создана или не запущена. Проверьте `dkktPort = 4042`, службы ЕСМ и подготовленную программой диагностику.
- `1015` — нет цепочки ДККТ/оркестра либо указан неправильный `dkktPort`.
- `2046` — экземпляр ЕСМ ещё не зарегистрирован или не запущен.
- `Порт ... уже занят` — программа остановилась до создания нового локального комплекта. Освободите указанный порт либо измените доступный порт ЛМ в предварительном плане.
- `Требуется очистка` — перезагрузите компьютер и нажмите `Завершить очистку`.
- Другая версия установщика — используйте точные поддерживаемые файлы; программа не экспериментирует с неизвестной версией на рабочей кассе.

## 8. Журнал и диагностика

Краткие состояния видны на форме. Полный журнал сохраняется в:

```text
%LOCALAPPDATA%\EsmTspiotTool\Logs
```

Файлы старше 14 дней удаляются автоматически. Кнопка `Скопировать диагностику` маскирует ИНН, номера ККТ/ФН, пользовательские пути и типовые секреты. Не отправляйте посторонним исходный файл журнала без проверки.

## 9. Главное правило

Перед запуском сравните серийные номера, ИНН, `softPort` и endpoint ЛМ во всей предварительной таблице. При ошибке не клонируйте каталоги и не создавайте службы вручную через `sc.exe`: сначала посмотрите журнал, при необходимости выполните `Удалить всё созданное`, перезагрузите компьютер и повторите настройку теми же проверенными пакетами.
