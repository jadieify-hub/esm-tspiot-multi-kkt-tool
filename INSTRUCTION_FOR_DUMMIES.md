# Простая инструкция: несколько ККТ и отдельные контроллеры ЕСМ

Издатель и владелец: KRS. Автор: Руслан Керусов. Copyright © 2026 KRS. Все права защищены.

## Что делает эта версия

- регистрирует одну или несколько ККТ в ЕСМ;
- при разных ИНН поочерёдно открывает конкретные VCOM АТОЛ и обходит ошибку 1026;
- после регистрации отдельно создаёт по одной прямой службе официального контроллера ЛМ для каждой ККТ;
- не устанавливает и не инициализирует сами ЛМ ЧЗ — это можно выполнить позже, регистрация и контроллеры от этого не зависят.

## Что подготовить

1. Распакуйте все семь файлов компактной поставки в `C:\Program Files\KRS\MultiKKT`.
2. Штатно установите ЕСМ/ТС ПИоТ, Драйвер ККТ АТОЛ v.10 и официальный `ЕСП Контроллер ЛМ ЧЗ 1.6.4.0`.
3. Подключите ККТ по USB/VCOM. Для нескольких касс не используйте `USB:auto`: каждая касса должна определяться своим VCOM.
4. Проверьте верхнюю строку программы. Обычные значения: адрес ЕСМ `http://127.0.0.1:51077`, `dkktPort` — `4042`.

## Автоматическая регистрация

1. Закройте Frontol и `Тест драйвера ККТ`, если они держат канал кассы.
2. Откройте `Автоматическая настройка` и нажмите `Зарегистрировать и настроить`.
3. Программа найдёт конкретные VCOM, сверит серийники и будет подключать кассы строго по одной.
4. Уже зарегистрированные кассы пропускаются. Успешная регистрация не откатывается из-за ошибки другой кассы.
5. Дождитесь итога регистрации.

Для одной ККТ используется тот же поток без лишних окон. Если кассу удерживает другое ПО, программа предложит закрыть его и повторить, но не будет завершать чужой процесс сама.

## Настройка контроллеров

После регистрации появится отдельное предупреждение: настройка контроллеров кратковременно перезапустит экземпляры ЕСМ.

- `Да` — настроить контроллеры сейчас;
- `Нет` — отложить фазу; зарегистрированные ККТ останутся в ЕСМ.

Порты назначаются так:

| ordinal | Служба | gRPC | REST | Будущий ЛМ |
| ---: | --- | ---: | ---: | ---: |
| 1 | `esm-lm-controller` | `50063` | `5063` | `5995` |
| 2 | `esm-lm-controller-2` | `50064` | `5064` | `6995` |
| 3 | `esm-lm-controller-3` | `50065` | `5065` | `7995` |

Первая ККТ использует штатную службу. Для следующих создаются прямые SCM-клоны того же проверенного `lmcontroller.exe`, каждый со своим профилем и сертификатом. Постоянных KRS-helper/supervisor и собственных Erlang/EPMD-процессов не остаётся.

Ошибки ЕСМ `2025` и `2055` до установки ЛМ допустимы: контроллер уже готов, а ЛМ можно поставить и инициализировать позже.

## Как повторить или удалить

- На вкладке `ЛМ ЧЗ` нажмите `Обновить`, затем `Создать / обновить контроллеры`, чтобы повторить отложенную фазу.
- `Удалить все контроллеры` удаляет только подтверждённые клоны MultiKKT и восстанавливает изменённые программой экземплярные YAML.
- Штатная служба `esm-lm-controller`, установленный вендорный бинарник и записи ККТ в ЕСМ не удаляются.
- Если YAML изменён после нашей настройки, программа не перезаписывает его автоматически и сообщает о конфликте.

## Частые ошибки

- `1026` — в `dkktList` одновременно видны кассы разных ИНН. Закройте удерживающее кассы ПО и повторите автоматический VCOM-сценарий.
- `2025`/`2055` — ЛМ отсутствует или не инициализирован; регистрации и готовности прямого контроллера это не мешает.
- Контроллер `1.6.4.0` не найден или не прошёл проверку — установите официальный пакет нужной версии; вендорные файлы в MultiKKT не входят.
- Порт занят — проверьте, не осталась ли чужая служба с ожидаемым именем. Чужие службы программа не усыновляет и не удаляет.

## Журнал

Полный журнал находится в `%LOCALAPPDATA%\EsmTspiotTool\Logs`. Для полевой проверки используйте `FIELD_TEST_1.6.4.0.md` из поставки. Перед отправкой журнала третьим лицам маскируйте ИНН, серийные номера и другие данные клиента.
